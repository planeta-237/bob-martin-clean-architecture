interface:
  display_name: "Bob Martin Clean Architecture"
  short_description: "Design and review canonical Clean Architecture"
  default_prompt: "Use $bob-martin-clean-architecture to design or review Robert C. Martin's layered Clean Architecture with entities, use cases, interface adapters, frameworks, dependency direction, and testability."

policy:
  allow_implicit_invocation: true
